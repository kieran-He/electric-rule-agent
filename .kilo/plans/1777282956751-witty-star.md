# 从 JSON 导入向量库计划

## 目标

将 `data/processed/*.json` 的数据导入到 ChromaDB 向量库，确保字段映射正确（`doc_name`、`page_start`、`page_end`）。

---

## 问题分析

### 当前状态

| 数据源 | 状态 | 问题 |
|--------|------|------|
| BM25 索引 | ✅ 557条 | 字段已修复 |
| ChromaDB | ❌ 旧数据/空 | metadata 缺少 `doc_name`, `page_start`, `page_end` |
| MySQL | 待确认 | Clause 表有 `page_start/page_end` 字段 |

### ingestion_pipeline.py 字段缺失

**`_build_metadata` (line 162-175)**:
```python
# 缺少:
"doc_name": doc.doc_name,
"page_start": clause_data.get("page_start"),
"page_end": clause_data.get("page_end"),
```

**`_build_metadata_from_clause` (line 197-211)**:
```python
# 缺少:
"doc_name": doc.doc_name,
"page_start": clause.page_start,
"page_end": clause.page_end,
```

---

## 修改方案

### 1. 修复 ingestion_pipeline.py

**修改 `_build_metadata` (line 162-175)**:
```python
def _build_metadata(self, clause_data: dict[str, Any], doc: Document) -> dict[str, str]:
    return {
        "province_code": doc.province_code,
        "doc_id": str(doc.id),
        "doc_name": doc.doc_name[:200] if doc.doc_name else "",
        "source_name": doc.doc_name[:200] if doc.doc_name else "",  # 兼容
        "source_path": doc.source_file,
        "file_hash": doc.file_hash,
        "doc_title": doc.doc_name[:200] if doc.doc_name else "",
        "effective_date": str(doc.effective_date or ""),
        "policy_level": doc.status,
        "doc_type": doc.doc_type,
        "article_no": clause_data.get("article_no", ""),
        "title_path": clause_data.get("title_path", "")[:500] if clause_data.get("title_path") else "",
        "page_start": str(clause_data.get("page_start") or ""),
        "page_end": str(clause_data.get("page_end") or ""),
    }
```

**修改 `_build_metadata_from_clause` (line 197-211)**:
```python
def _build_metadata_from_clause(self, clause: Clause) -> dict[str, str]:
    doc = clause.document
    return {
        "province_code": doc.province_code,
        "doc_id": str(doc.id),
        "doc_name": doc.doc_name[:200] if doc.doc_name else "",
        "source_name": doc.doc_name[:200] if doc.doc_name else "",  # 兼容
        "source_path": doc.source_file,
        "file_hash": doc.file_hash,
        "doc_title": doc.doc_name[:200] if doc.doc_name else "",
        "effective_date": str(doc.effective_date or ""),
        "policy_level": doc.status,
        "doc_type": doc.doc_type,
        "article_no": clause.article_no or "",
        "title_path": clause.title_path[:500] if clause.title_path else "",
        "page_start": str(clause.page_start or ""),
        "page_end": str(clause.page_end or ""),
    }
```

---

## 导入步骤

### 步骤 1: 清空旧数据

**清空 MySQL Document/Clause 表**:
```python
from app.db.session import session_factory
from app.db.models.document import Document
from app.db.models.clause import Clause

with session_factory() as db:
    db.query(Clause).delete()
    db.query(Document).delete()
    db.commit()
    print("MySQL Document/Clause 表已清空")
```

**清空 ChromaDB kb_sn collection**:
```python
from app.config import settings
import chromadb

client = chromadb.PersistentClient(path=settings.chroma_path)
try:
    client.delete_collection('kb_sn')
    print("ChromaDB kb_sn collection 已删除")
except Exception as e:
    print(f"删除 collection 失败（可能不存在）: {e}")
```

### 步骤 2: 从 JSON 导入

**方式 A: 使用 API**:
```bash
curl -X POST http://localhost:8000/api/ingest/documents \
  -H "Content-Type: application/json" \
  -d '{"path": "data/processed", "province_code": "SN", "rebuild_index": true}'
```

**方式 B: 直接调用脚本**:
```python
# 保存为 scripts/import_from_json.py
from pathlib import Path
from app.config import settings
from app.db.session import session_factory
from app.services.ingest.ingestion_pipeline import IngestionPipeline

with session_factory() as db:
    pipeline = IngestionPipeline(db, settings)
    result = pipeline.ingest_path(
        Path("data/processed"),
        province_code="SN",
        rebuild_index=True
    )
    db.commit()
    print(f"导入完成: {result}")
```

运行:
```bash
python scripts/import_from_json.py
```

**或使用完整清理+导入脚本**:
```python
# 保存为 scripts/rebuild_knowledge_base.py
"""
重建知识库：清空 MySQL + ChromaDB，从 JSON 导入
"""
from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from app.config import settings
from app.db.session import session_factory
from app.db.models.document import Document
from app.db.models.clause import Clause
from app.services.ingest.ingestion_pipeline import IngestionPipeline
import chromadb

def clear_mysql():
    """清空 MySQL Document/Clause 表"""
    with session_factory() as db:
        deleted_clauses = db.query(Clause).delete()
        deleted_docs = db.query(Document).delete()
        db.commit()
        print(f"MySQL: 已删除 {deleted_docs} 个文档, {deleted_clauses} 个条款")

def clear_chroma():
    """清空 ChromaDB kb_sn collection"""
    client = chromadb.PersistentClient(path=settings.chroma_path)
    try:
        client.delete_collection('kb_sn')
        print("ChromaDB: kb_sn collection 已删除")
    except Exception as e:
        print(f"ChromaDB: 删除失败（可能不存在）: {e}")

def import_from_json():
    """从 JSON 导入数据"""
    with session_factory() as db:
        pipeline = IngestionPipeline(db, settings)
        result = pipeline.ingest_path(
            Path("data/processed"),
            province_code="SN",
            rebuild_index=True
        )
        db.commit()
        print(f"导入完成: {result}")
        return result

def verify():
    """验证导入结果"""
    client = chromadb.PersistentClient(path=settings.chroma_path)
    try:
        collection = client.get_collection("kb_sn")
        count = collection.count()
        print(f"验证: ChromaDB kb_sn 包含 {count} 条文档")
        
        # 检查 metadata 字段
        sample = collection.peek(limit=1)
        if sample['metadatas']:
            fields = list(sample['metadatas'][0].keys())
            print(f"Metadata 字段: {fields}")
            required = ['doc_name', 'page_start', 'page_end', 'title_path']
            missing = [f for f in required if f not in fields]
            if missing:
                print(f"警告: 缺少字段 {missing}")
            else:
                print("验证成功: 所有必需字段都存在")
    except Exception as e:
        print(f"验证失败: {e}")

def main():
    print("=" * 50)
    print("重建知识库")
    print("=" * 50)
    
    clear_mysql()
    clear_chroma()
    import_from_json()
    verify()
    
    print("=" * 50)
    print("重建完成")

if __name__ == "__main__":
    main()
```

---

## 验证

导入后检查:
```python
from app.config import settings
import chromadb

client = chromadb.PersistentClient(path=settings.chroma_path)
collection = client.get_collection("kb_sn")
print(f"文档数量: {collection.count()}")

# 检查 metadata 字段
sample = collection.peek(limit=1)
print(f"Metadata 字段: {sample['metadatas'][0].keys()}")
```

---

## 文件修改清单

| 文件 | 修改 |
|------|------|
| `app/services/ingest/ingestion_pipeline.py` | `_build_metadata` 添加 `doc_name`, `page_start`, `page_end` |
| `app/services/ingest/ingestion_pipeline.py` | `_build_metadata_from_clause` 添加 `doc_name`, `page_start`, `page_end` |
| `scripts/rebuild_knowledge_base.py` | 创建完整重建脚本（清空MySQL + ChromaDB + 导入JSON） |

---

## 执行顺序

1. 修改 `ingestion_pipeline.py` 中的两个 metadata 构建方法
2. 创建 `scripts/rebuild_knowledge_base.py` 脚本
3. 运行重建脚本

---

## 预期结果

导入成功后:
- ChromaDB `kb_sn` collection 包含 ~557 条文档
- metadata 包含 `doc_name`, `page_start`, `page_end` 字段
- 飞书参考文献格式正确显示：`陕西2026年电力市场方案 > 十二 > 风险防控 (P15-17)`