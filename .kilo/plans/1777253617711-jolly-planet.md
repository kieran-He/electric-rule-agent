# 问答助手系统完整性完善方案

## 概述

本方案旨在完善问答助手系统的核心功能，包括上下文管理、Trace 记录、日志监控、缓存机制等关键模块。

---

## 优先级 1: 核心功能

### 1.1 上下文管理集成

**问题**: `SessionStore` (内存) 与 `ConversationState` (数据库) 并存但未集成，多轮对话 history 硬编码为空。

**修改文件**:
- `app/services/query_service.py`
- `app/langchain/orchestrator.py`
- `app/langchain/orchestrator_hybrid.py`
- `app/services/conversation_service.py` (新建)

**实现步骤**:

#### Step 1: 创建 ConversationService

```python
# app/services/conversation_service.py
from typing import Callable
from sqlalchemy.orm import Session
from app.db.repositories.conversation_repo import ConversationRepository
from app.db.models.conversation_state import ConversationState

class ConversationService:
    def __init__(self, session_factory: Callable[[], Session], max_history_turns: int = 6):
        self.session_factory = session_factory
        self.max_history_turns = max_history_turns

    def get_or_create(self, session_id: str) -> ConversationState:
        with self.session_factory() as db:
            repo = ConversationRepository(db)
            state = repo.get(session_id)
            if state is None:
                state = ConversationState(session_id=session_id)
                db.add(state)
                db.commit()
            return state

    def get_history(self, session_id: str) -> list[str]:
        state = self.get_or_create(session_id)
        # 从 last_question 和数据库中重建历史
        history = []
        if state.last_question:
            history.append(f"Q: {state.last_question}")
        return history

    def append_turn(self, session_id: str, user_query: str, bot_reply: str, intent: str = None):
        with self.session_factory() as db:
            repo = ConversationRepository(db)
            state = repo.get(session_id)
            if state is None:
                state = ConversationState(session_id=session_id)
            state.last_question = user_query
            state.last_intent = intent
            state.updated_at = datetime.utcnow()
            repo.upsert(state)
            db.commit()

    def update_context(self, session_id: str, province_code: str = None, market_type: str = None):
        with self.session_factory() as db:
            repo = ConversationRepository(db)
            state = repo.get(session_id)
            if state:
                if province_code:
                    state.province_code = province_code
                if market_type:
                    state.market_type = market_type
                repo.upsert(state)
                db.commit()
```

#### Step 2: 修改 QueryService

```python
# app/services/query_service.py
class QueryService:
    def __init__(self, settings, session_factory: Callable[[], Session]):
        self.settings = settings
        self.session_factory = session_factory
        self.conversation_service = ConversationService(session_factory)

    def answer(self, req: QueryRequest) -> QueryAnswer:
        with self.session_factory() as db:
            # 获取会话历史
            history = self.conversation_service.get_history(req.session_id)
            
            orchestrator = self._get_orchestrator(db)
            response = orchestrator.run(req, history=history)  # 传递 history
            
            # 更新会话状态
            self.conversation_service.append_turn(
                session_id=req.session_id,
                user_query=req.query,
                bot_reply=response.answer,
                intent=response.intent
            )
            
            db.commit()
            return response
```

#### Step 3: 修改 Orchestrator 接收 history

```python
# app/langchain/orchestrator_hybrid.py
def run(self, req: QueryRequest, history: list[str] = None) -> QueryAnswer:
    trace_id = f"trace_{uuid.uuid4().hex[:12]}"
    
    chunks = self._retrieve(req.query, req.province_codes, req.top_k)
    
    # 传递 history 到生成器
    province_code = req.province_codes[0] if req.province_codes else "SN"
    answer = self._generate_answer(req.query, chunks, province_code, history or [])
    
    ...

def _generate_answer(self, query: str, chunks: List[PolicyChunk], province_code: str, history: list[str]) -> str:
    if not chunks:
        return "未检索到相关文档..."
    
    provincial_context = format_chunks_for_context(chunks)
    global_context = "- 无通用证据"
    
    # 使用实际 history
    history_text = "\n".join(history[-self._settings.conversation_ttl_minutes:]) if history else ""
    
    user_content = f"""问题: {query}

省级证据({province_code}):
{provincial_context}

通用证据:
{global_context}

历史对话:
{history_text}

请根据上述证据回答问题。"""
    ...
```

#### Step 4: 添加依赖注入

```python
# app/core/dependency.py
from app.services.conversation_service import ConversationService

@lru_cache
def get_conversation_service() -> ConversationService:
    init_db()
    return ConversationService(session_factory=SessionLocal)
```

---

### 1.2 Trace 写入实现

**问题**: `TraceService` 只有 `get_trace()`，没有 `save_trace()`，TraceRecord 永远为空。

**修改文件**:
- `app/services/trace_service.py`
- `app/langchain/orchestrator_hybrid.py`

**实现步骤**:

#### Step 1: 添加 save_trace 方法

```python
# app/services/trace_service.py
import time

class TraceService:
    def __init__(self, session_factory: Callable[[], Session]):
        self.session_factory = session_factory

    def save_trace(
        self,
        trace_id: str,
        session_id: str,
        raw_query: str,
        rewritten_query: str | None = None,
        intent: str | None = None,
        filters: dict = None,
        retrieved_doc_ids: list[int] = None,
        rerank_scores: list[float] = None,
        used_clause_ids: list[int] = None,
        final_doc_ids: list[int] = None,
        latency_ms: int = 0,
    ) -> TraceRecord:
        with self.session_factory() as db:
            trace = TraceRecord(
                trace_id=trace_id,
                session_id=session_id,
                raw_query=raw_query,
                rewritten_query=rewritten_query,
                intent=intent,
                filters=json.dumps(filters or {}),
                retrieved_doc_ids=json.dumps(retrieved_doc_ids or []),
                rerank_scores=json.dumps(rerank_scores or []),
                used_clause_ids=json.dumps(used_clause_ids or []),
                final_doc_ids=json.dumps(final_doc_ids or []),
                latency_ms=str(latency_ms),
            )
            db.add(trace)
            db.commit()
            return trace
```

#### Step 2: 在 Orchestrator 中记录 Trace

```python
# app/langchain/orchestrator_hybrid.py
import time
from app.services.trace_service import TraceService

class HybridQAOrchestrator:
    def run(self, req: QueryRequest, history: list[str] = None, trace_service: TraceService = None) -> QueryAnswer:
        trace_id = f"trace_{uuid.uuid4().hex[:12]}"
        start_time = time.time()
        
        # 检索前记录
        rewrite_result = None
        if self.hybrid_retriever and self.hybrid_retriever.query_rewriter:
            rewrite_result = self.hybrid_retriever.query_rewriter.rewrite(req.query)
        
        chunks = self._retrieve(req.query, req.province_codes, req.top_k)
        
        province_code = req.province_codes[0] if req.province_codes else "SN"
        answer = self._generate_answer(req.query, chunks, province_code, history or [])
        
        citations = self._build_citations(chunks) if req.need_citation else []
        used_documents = [c.doc_name for c in citations]
        
        latency_ms = int((time.time() - start_time) * 1000)
        
        # 保存 Trace
        if trace_service:
            trace_service.save_trace(
                trace_id=trace_id,
                session_id=req.session_id,
                raw_query=req.query,
                rewritten_query=rewrite_result.rewritten_query if rewrite_result and rewrite_result.triggered else None,
                intent="clause_qa",
                retrieved_doc_ids=[int(c.metadata.get("doc_id", 0)) for c in chunks],
                rerank_scores=[c.score for c in chunks],
                latency_ms=latency_ms,
            )
        
        return QueryAnswer(
            answer=answer,
            citations=citations,
            intent="clause_qa",
            confidence=0.8 if chunks else 0.3,
            used_documents=used_documents,
            trace_id=trace_id,
            flow=None,
            warnings=[] if chunks else ["未检索到相关文档"],
        )
```

#### Step 3: 在 QueryService 中传递 TraceService

```python
# app/services/query_service.py
def answer(self, req: QueryRequest) -> QueryAnswer:
    with self.session_factory() as db:
        history = self.conversation_service.get_history(req.session_id)
        
        trace_service = TraceService(self.session_factory)
        orchestrator = self._get_orchestrator(db)
        
        response = orchestrator.run(req, history=history, trace_service=trace_service)
        
        self.conversation_service.append_turn(...)
        db.commit()
        return response
```

---

## 优先级 2: 可观测性

### 2.1 结构化日志

**问题**: 纯文本日志无法对接 ELK/Loki，缺少 trace_id 关联。

**修改文件**:
- `app/core/logger.py`
- `app/core/logging_context.py` (新建)

**实现步骤**:

#### Step 1: 创建结构化日志格式

```python
# app/core/logger.py
import logging
import json
from datetime import datetime

class StructuredFormatter(logging.Formatter):
    def format(self, record: logging.LogRecord) -> str:
        log_obj = {
            "timestamp": datetime.utcnow().isoformat() + "Z",
            "level": record.levelname,
            "logger": record.name,
            "message": record.getMessage(),
            "module": record.module,
            "function": record.funcName,
            "line": record.lineno,
        }
        
        # 添加上下文信息
        if hasattr(record, "trace_id"):
            log_obj["trace_id"] = record.trace_id
        if hasattr(record, "session_id"):
            log_obj["session_id"] = record.session_id
        if hasattr(record, "user_id"):
            log_obj["user_id"] = record.user_id
        
        if record.exc_info:
            log_obj["exception"] = self.formatException(record.exc_info)
        
        return json.dumps(log_obj, ensure_ascii=False)

def configure_logging() -> None:
    root = logging.getLogger()
    if root.handlers:
        return
    root.setLevel(settings.log_level.upper())
    
    # 结构化日志文件
    structured_handler = RotatingFileHandler(
        settings.log_file.replace(".log", "_structured.json"),
        maxBytes=5 * 1024 * 1024,
        backupCount=5,
        encoding="utf-8",
    )
    structured_handler.setFormatter(StructuredFormatter())
    root.addHandler(structured_handler)
    
    # 控制台保留简单格式
    console = logging.StreamHandler()
    console.setFormatter(logging.Formatter("%(asctime)s | %(levelname)s | %(name)s | %(message)s"))
    root.addHandler(console)
```

#### Step 2: 创建日志上下文工具

```python
# app/core/logging_context.py
import contextvars
from typing import Optional

_trace_id: contextvars.ContextVar[Optional[str]] = contextvars.ContextVar("trace_id", default=None)
_session_id: contextvars.ContextVar[Optional[str]] = contextvars.ContextVar("session_id", default=None)

def set_trace_id(trace_id: str):
    _trace_id.set(trace_id)

def set_session_id(session_id: str):
    _session_id.set(session_id)

def get_trace_id() -> Optional[str]:
    return _trace_id.get()

def get_session_id() -> Optional[str]:
    return _session_id.get()

class LoggingContextFilter(logging.Filter):
    def filter(self, record: logging.LogRecord) -> bool:
        record.trace_id = get_trace_id()
        record.session_id = get_session_id()
        return True
```

#### Step 3: 在请求中设置上下文

```python
# app/api/routes_query.py
from app.core.logging_context import set_trace_id, set_session_id

@router.post("", response_model=QueryAnswer)
def query_route(req: QueryRequest, service: QueryService = Depends(get_query_service)) -> QueryAnswer:
    trace_id = f"trace_{uuid.uuid4().hex[:12]}"
    set_trace_id(trace_id)
    set_session_id(req.session_id)
    
    try:
        return service.answer(req)
    except AppError as exc:
        raise HTTPException(status_code=exc.status_code, detail=exc.message) from exc
```

---

### 2.2 性能埋点

**问题**: 缺少 latency、token count、检索耗时等指标。

**修改文件**:
- `app/core/metrics.py` (新建)
- `app/api/routes_metrics.py` (新建)
- `app/langchain/orchestrator_hybrid.py`

**实现步骤**:

#### Step 1: 创建 Metrics 收集器

```python
# app/core/metrics.py
from dataclasses import dataclass, field
from threading import Lock
from typing import Dict, List
import time

@dataclass
class MetricPoint:
    timestamp: float
    value: float

@dataclass
class MetricsStore:
    _lock: Lock = field(default_factory=Lock)
    _latency_samples: List[MetricPoint] = field(default_factory=list)
    _retrieval_latency: List[MetricPoint] = field(default_factory=list)
    _llm_latency: List[MetricPoint] = field(default_factory=list)
    _token_counts: List[MetricPoint] = field(default_factory=list)
    _query_counts: Dict[str, int] = field(default_factory=dict)
    _error_counts: Dict[str, int] = field(default_factory=dict)
    
    def record_latency(self, latency_ms: float, category: str = "total"):
        with self._lock:
            point = MetricPoint(timestamp=time.time(), value=latency_ms)
            if category == "total":
                self._latency_samples.append(point)
            elif category == "retrieval":
                self._retrieval_latency.append(point)
            elif category == "llm":
                self._llm_latency.append(point)
    
    def record_tokens(self, input_tokens: int, output_tokens: int):
        with self._lock:
            self._token_counts.append(MetricPoint(
                timestamp=time.time(),
                value=input_tokens + output_tokens
            ))
    
    def record_query(self, province_code: str):
        with self._lock:
            self._query_counts[province_code] = self._query_counts.get(province_code, 0) + 1
    
    def record_error(self, error_type: str):
        with self._lock:
            self._error_counts[error_type] = self._error_counts.get(error_type, 0) + 1
    
    def get_summary(self) -> dict:
        with self._lock:
            def avg(points: List[MetricPoint]) -> float:
                if not points:
                    return 0.0
                return sum(p.value for p in points[-100:]) / len(points[-100:])
            
            return {
                "latency_avg_ms": avg(self._latency_samples),
                "retrieval_latency_avg_ms": avg(self._retrieval_latency),
                "llm_latency_avg_ms": avg(self._llm_latency),
                "tokens_avg": avg(self._token_counts),
                "query_counts": dict(self._query_counts),
                "error_counts": dict(self._error_counts),
                "samples_count": len(self._latency_samples),
            }

metrics_store = MetricsStore()
```

#### Step 2: 在 Orchestrator 中埋点

```python
# app/langchain/orchestrator_hybrid.py
from app.core.metrics import metrics_store

def run(self, req: QueryRequest, ...) -> QueryAnswer:
    start_time = time.time()
    retrieval_start = time.time()
    
    chunks = self._retrieve(...)
    
    retrieval_latency = (time.time() - retrieval_start) * 1000
    metrics_store.record_latency(retrieval_latency, "retrieval")
    
    llm_start = time.time()
    answer = self._generate_answer(...)
    llm_latency = (time.time() - llm_start) * 1000
    metrics_store.record_latency(llm_latency, "llm")
    
    total_latency = (time.time() - start_time) * 1000
    metrics_store.record_latency(total_latency, "total")
    metrics_store.record_query(req.province_codes[0] if req.province_codes else "SN")
    
    ...
```

#### Step 3: 创建 Metrics API

```python
# app/api/routes_metrics.py
from fastapi import APIRouter
from app.core.metrics import metrics_store

router = APIRouter(prefix="/metrics", tags=["metrics"])

@router.get("")
def get_metrics() -> dict:
    return metrics_store.get_summary()

@router.get("/health")
def get_health() -> dict:
    from app.repository import ChromaPolicyRepository
    from app.config import settings
    
    repo = ChromaPolicyRepository(
        persist_directory=settings.chroma_path,
        embedding_model_name=settings.embedding_model,
    )
    
    return {
        "status": "ok",
        "chroma_ready": repo.ready,
        "reranker_loaded": reranker_cache.is_loaded(),
        "metrics_samples": len(metrics_store._latency_samples),
    }
```

---

### 2.3 健康检查增强

**问题**: `/health` 只返回 `status: ok`，缺少组件状态检查。

**修改文件**:
- `app/api/routes_admin.py`
- `app/core/health_check.py` (新建)

**实现步骤**:

#### Step 1: 创建健康检查服务

```python
# app/core/health_check.py
from dataclasses import dataclass

@dataclass
class HealthStatus:
    component: str
    status: str  # "ok", "degraded", "error"
    message: str = ""
    latency_ms: int = 0

class HealthChecker:
    def check_all(self) -> dict:
        return {
            "overall": self._overall_status(),
            "components": [
                self.check_database(),
                self.check_chroma(),
                self.check_llm(),
                self.check_reranker(),
                self.check_bm25(),
            ]
        }
    
    def check_database(self) -> HealthStatus:
        try:
            from app.db.session import engine
            with engine.connect() as conn:
                conn.execute("SELECT 1")
            return HealthStatus("database", "ok", "SQLite connected")
        except Exception as e:
            return HealthStatus("database", "error", str(e)[:100])
    
    def check_chroma(self) -> HealthStatus:
        try:
            from app.repository import ChromaPolicyRepository
            from app.config import settings
            repo = ChromaPolicyRepository(
                persist_directory=settings.chroma_path,
                embedding_model_name=settings.embedding_model,
            )
            if repo.ready:
                collections = repo._client.list_collections()
                return HealthStatus("chroma", "ok", f"{len(collections)} collections")
            return HealthStatus("chroma", "error", repo.init_error or "not ready")
        except Exception as e:
            return HealthStatus("chroma", "error", str(e)[:100])
    
    def check_llm(self) -> HealthStatus:
        try:
            from app.generator import LLMClient
            import os
            client = LLMClient(
                api_key=os.getenv("LLM_API_KEY", ""),
                endpoint=os.getenv("LLM_ENDPOINT", ""),
                model=os.getenv("LLM_MODEL", ""),
            )
            if client.ready:
                return HealthStatus("llm", "ok", "API key configured")
            return HealthStatus("llm", "degraded", "No API key, using mock")
        except Exception as e:
            return HealthStatus("llm", "error", str(e)[:100])
    
    def check_reranker(self) -> HealthStatus:
        from app.langchain.reranker_cache import reranker_cache
        if reranker_cache.is_loaded():
            return HealthStatus("reranker", "ok", f"model: {reranker_cache.get_model_name()}")
        return HealthStatus("reranker", "degraded", "not loaded")
    
    def check_bm25(self) -> HealthStatus:
        from app.langchain.bm25_indexer import BM25Indexer
        indexer = BM25Indexer()
        if indexer.is_available():
            return HealthStatus("bm25", "ok", f"{indexer.doc_count()} docs indexed")
        return HealthStatus("bm25", "degraded", "index not built")
    
    def _overall_status(self) -> str:
        statuses = [c.status for c in self.check_all()["components"]]
        if "error" in statuses:
            return "error"
        if "degraded" in statuses:
            return "degraded"
        return "ok"

health_checker = HealthChecker()
```

#### Step 2: 修改 Admin 路由

```python
# app/api/routes_admin.py
from app.core.health_check import health_checker

@router.get("/health")
def health_check() -> dict:
    return health_checker.check_all()
```

---

## 优先级 3: 性能优化

### 3.1 查询结果缓存

**问题**: 相同查询重复计算，无缓存机制。

**修改文件**:
- `app/core/cache.py` (新建)
- `app/services/query_service.py`

**实现步骤**:

#### Step 1: 创建缓存服务

```python
# app/core/cache.py
import hashlib
import json
from dataclasses import dataclass, field
from threading import Lock
from typing import Dict, Optional
import time

@dataclass
class CacheEntry:
    value: any
    timestamp: float
    ttl_seconds: int

@dataclass
class QueryCache:
    _lock: Lock = field(default_factory=Lock)
    _cache: Dict[str, CacheEntry] = field(default_factory=dict)
    default_ttl: int = 300  # 5 minutes
    
    def _hash_query(self, query: str, province_codes: list[str], top_k: int) -> str:
        key = json.dumps({
            "query": query.strip().lower(),
            "provinces": sorted(province_codes),
            "top_k": top_k
        }, ensure_ascii=False)
        return hashlib.sha256(key.encode()).hexdigest()[:16]
    
    def get(self, query: str, province_codes: list[str], top_k: int) -> Optional[any]:
        key = self._hash_query(query, province_codes, top_k)
        with self._lock:
            entry = self._cache.get(key)
            if entry is None:
                return None
            if time.time() - entry.timestamp > entry.ttl_seconds:
                del self._cache[key]
                return None
            return entry.value
    
    def set(self, query: str, province_codes: list[str], top_k: int, value: any, ttl: int = None):
        key = self._hash_query(query, province_codes, top_k)
        with self._lock:
            self._cache[key] = CacheEntry(
                value=value,
                timestamp=time.time(),
                ttl_seconds=ttl or self.default_ttl
            )
    
    def clear(self):
        with self._lock:
            self._cache.clear()
    
    def stats(self) -> dict:
        with self._lock:
            return {
                "entries": len(self._cache),
                "oldest_age": min(
                    time.time() - e.timestamp for e in self._cache.values()
                ) if self._cache else 0,
            }

query_cache = QueryCache()
```

#### Step 2: 在 QueryService 中使用缓存

```python
# app/services/query_service.py
from app.core.cache import query_cache

class QueryService:
    def answer(self, req: QueryRequest) -> QueryAnswer:
        # 尝试缓存命中
        cached = query_cache.get(req.query, req.province_codes, req.top_k)
        if cached:
            logger.info(f"Cache hit for query: {req.query[:50]}")
            return cached
        
        with self.session_factory() as db:
            history = self.conversation_service.get_history(req.session_id)
            orchestrator = self._get_orchestrator(db)
            response = orchestrator.run(req, history=history)
            
            # 写入缓存
            query_cache.set(req.query, req.province_codes, req.top_k, response)
            
            self.conversation_service.append_turn(...)
            db.commit()
            return response
```

---

## 优先级 4: 测试与文档

### 4.1 关键模块测试

**缺失测试文件**:
- `tests/test_hybrid_retriever.py`
- `tests/test_conversation_service.py`
- `tests/test_trace_service.py`
- `tests/test_orchestrator.py`

**示例测试**:

```python
# tests/test_conversation_service.py
import pytest
from app.services.conversation_service import ConversationService
from app.db.session import SessionLocal, init_db

@pytest.fixture
def conversation_service():
    init_db()
    return ConversationService(session_factory=SessionLocal)

def test_get_or_create(conversation_service):
    state = conversation_service.get_or_create("test_session_1")
    assert state.session_id == "test_session_1"
    
def test_append_turn(conversation_service):
    conversation_service.append_turn(
        session_id="test_session_2",
        user_query="陕西交易规则",
        bot_reply="根据规则...",
        intent="clause_qa"
    )
    history = conversation_service.get_history("test_session_2")
    assert len(history) == 1
    assert "陕西交易规则" in history[0]

def test_update_context(conversation_service):
    conversation_service.update_context(
        session_id="test_session_3",
        province_code="GD"
    )
    state = conversation_service.get_or_create("test_session_3")
    assert state.province_code == "GD"
```

```python
# tests/test_trace_service.py
import pytest
from app.services.trace_service import TraceService
from app.db.session import SessionLocal, init_db

@pytest.fixture
def trace_service():
    init_db()
    return TraceService(session_factory=SessionLocal)

def test_save_and_get_trace(trace_service):
    trace = trace_service.save_trace(
        trace_id="trace_test_001",
        session_id="session_001",
        raw_query="测试查询",
        latency_ms=100
    )
    assert trace.trace_id == "trace_test_001"
    
    retrieved = trace_service.get_trace("trace_test_001")
    assert retrieved is not None
    assert retrieved.raw_query == "测试查询"
    assert retrieved.latency_ms == 100
```

---

### 4.2 会话 TTL 管理

**问题**: `ConversationRepository.clear_expired()` 定义但未调用。

**修改文件**:
- `app/services/session_cleanup.py` (新建)
- `app/main.py`

**实现步骤**:

#### Step 1: 创建清理任务

```python
# app/services/session_cleanup.py
import threading
import time
from datetime import datetime
import logging

logger = logging.getLogger(__name__)

class SessionCleanupTask:
    def __init__(self, interval_minutes: int = 30, ttl_minutes: int = 120):
        self.interval_minutes = interval_minutes
        self.ttl_minutes = ttl_minutes
        self._running = False
        self._thread = None
    
    def start(self):
        if self._running:
            return
        self._running = True
        self._thread = threading.Thread(target=self._run_loop, daemon=True)
        self._thread.start()
        logger.info(f"Session cleanup task started (interval={self.interval_minutes}min, ttl={self.ttl_minutes}min)")
    
    def stop(self):
        self._running = False
        if self._thread:
            self._thread.join(timeout=5)
    
    def _run_loop(self):
        while self._running:
            try:
                self._cleanup()
            except Exception as e:
                logger.error(f"Session cleanup error: {e}")
            
            time.sleep(self.interval_minutes * 60)
    
    def _cleanup(self):
        from app.db.repositories.conversation_repo import ConversationRepository
        from app.db.session import SessionLocal
        
        with SessionLocal() as db:
            repo = ConversationRepository(db)
            repo.clear_expired(self.ttl_minutes)
            db.commit()
            logger.info(f"Expired sessions cleared (ttl={self.ttl_minutes}min)")

cleanup_task = SessionCleanupTask()
```

#### Step 2: 在应用启动时运行

```python
# app/main.py
from app.services.session_cleanup import cleanup_task

@app.on_event("startup")
def startup_event():
    cleanup_task.start()

@app.on_event("shutdown")
def shutdown_event():
    cleanup_task.stop()
```

---

### 4.3 API 文档完善

**修改文件**:
- `app/api/routes_query.py`
- `app/schemas/query.py`

**实现步骤**:

```python
# app/api/routes_query.py
from fastapi import APIRouter, Depends, HTTPException
from app.schemas.query import QueryRequest, QueryRequestExample

router = APIRouter(
    prefix="/query",
    tags=["query"],
    responses={
        400: {"description": "Invalid request parameters"},
        500: {"description": "Internal server error"},
    }
)

@router.post(
    "",
    response_model=QueryAnswer,
    summary="Query Policy Knowledge Base",
    description="Execute a query against the policy knowledge base with hybrid retrieval.",
    responses={
        200: {
            "description": "Successful query response",
            "content": {
                "application/json": {
                    "example": {
                        "answer": "根据《陕西省电力市场交易实施细则》...",
                        "citations": [{"doc_name": "陕西规则.pdf", "excerpt": "..."}],
                        "intent": "clause_qa",
                        "confidence": 0.85,
                        "trace_id": "trace_abc123"
                    }
                }
            }
        }
    }
)
def query_route(req: QueryRequest = QueryRequestExample, ...):
    ...

# app/schemas/query.py
from pydantic import BaseModel, Field

class QueryRequest(BaseModel):
    query: str = Field(
        min_length=1,
        description="User query text",
        examples=["2026年陕西电力市场中长期交易流程是什么？"]
    )
    session_id: str = Field(
        min_length=1,
        description="Unique session identifier for conversation tracking",
        examples=["chat_123:user_456"]
    )
    province_codes: list[str] = Field(
        default_factory=lambda: ["SN"],
        description="Province codes to search (e.g., SN for Shaanxi)",
        examples=[["SN"], ["SN", "GD"]]
    )
    ...

QueryRequestExample = QueryRequest(
    query="2026年陕西电力市场中长期交易流程是什么？",
    session_id="example_session_001",
    province_codes=["SN"]
)
```

---

## 实施优先级排序

| 序号 | 模块 | 优先级 | 预估工时 | 依赖 |
|------|------|--------|----------|------|
| 1 | 上下文管理集成 | P0 | 2h | 无 |
| 2 | Trace 写入实现 | P0 | 1h | 无 |
| 3 | 健康检查增强 | P1 | 1h | 无 |
| 4 | 结构化日志 | P1 | 1h | 无 |
| 5 | 性能埋点 | P1 | 1h | 无 |
| 6 | 查询结果缓存 | P2 | 1h | 无 |
| 7 | 会话 TTL 管理 | P2 | 0.5h | 无 |
| 8 | 关键模块测试 | P2 | 3h | 1,2 |
| 9 | API 文档完善 | P3 | 0.5h | 无 |

**总预估工时**: 约 10 小时

---

## 验证方案

完成每个模块后需要验证：

### 上下文管理验证
```bash
# 测试多轮对话
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"query": "陕西交易规则", "session_id": "test_001"}'
  
# 第二轮应带上历史
curl -X POST http://localhost:8000/query \
  -H "Content-Type: application/json" \
  -d '{"query": "具体流程是什么", "session_id": "test_001"}'
```

### Trace 验证
```bash
# 查询后检查 trace
curl http://localhost:8000/query/trace/trace_xxx

# 应返回完整的 trace 信息
```

### 健康检查验证
```bash
curl http://localhost:8000/admin/health

# 应返回各组件状态
```

### Metrics 验证
```bash
curl http://localhost:8000/metrics

# 应返回 latency_avg_ms, query_counts 等
```

---

## 注意事项

1. **上下文管理**: 当前 `conversation_ttl_minutes` 配置为 120 分钟，但内存 SessionStore 无过期机制，需要统一使用数据库。

2. **Trace 写入**: 确保 `trace_id` 在 QueryAnswer 中返回，以便用户追踪。

3. **缓存 TTL**: 查询结果缓存默认 5 分钟，政策文档不频繁更新，可适当延长。

4. **会话清理**: 清理线程为 daemon 线程，应用退出时自动结束，但需要确保数据库连接正确关闭。

5. **测试隔离**: 测试需要独立的数据库实例，避免污染生产数据。