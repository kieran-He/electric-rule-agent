# README Update Plan

## Goal

Update README.md to comprehensively document the project as a power policy knowledge QA agent with Feishu bot integration, using bilingual (Chinese/English) format.

## Current README Issues

- Missing recent features: Query Rewrite, Query Expansion, Hybrid Retrieval details, RAGAS Evaluation APIs
- No architecture diagram placeholder
- No bilingual support
- Missing configuration details for BM25, Reranker, Query Rewrite/Expansion

## New README Structure

```markdown
# 电力政策知识问答 Agent / Power Policy Knowledge QA Agent

[问答示意图占位 / QA Demo Placeholder]

## 项目简介 / Overview

[中文] 本项目是一个基于 RAG 技术的电力政策知识问答系统，支持接入飞书机器人，提供多省份政策文档检索和智能问答服务。

[English] This project is a RAG-based power policy knowledge QA system with Feishu bot integration, supporting multi-province policy document retrieval and intelligent Q&A services.

## 核心特性 / Core Features

### 1. 智能检索 / Intelligent Retrieval
- Query Rewrite (LLM-powered query optimization)
- Query Expansion (semantic/synonym expansion)
- Hybrid Retrieval (Vector + BM25 + BGE Rerank)

### 2. 多省份支持 / Multi-Province Support
- Province-isolated knowledge bases
- Multi-province comparison queries
- Automatic province detection

### 3. 飞书集成 / Feishu Integration
- Real-time message handling
- Webhook verification
- Event deduplication

### 4. 效果评估 / Evaluation System
- RAGAS metrics (faithfulness, answer_relevancy, context_precision)
- Batch evaluation API
- A/B comparison workflow

## 架构示意 / Architecture

[架构图占位 / Architecture Diagram Placeholder]

## 快速开始 / Quick Start

[Installation, configuration, startup steps]

## 配置说明 / Configuration

[Key environment variables with bilingual descriptions]

## API 文档 / API Documentation

[All endpoints with bilingual descriptions]

## 评估系统 / Evaluation System

[Link to evaluation/README.md]

## 目录结构 / Project Structure

[Key directories and their purposes]
```

## Key Content to Add

### Configuration Section (New)

| Variable | Description |
|----------|-------------|
| `QUERY_REWRITE_ENABLED` | 启用 LLM 查询重写 / Enable LLM query rewrite |
| `QUERY_REWRITE_ALWAYS` | 强制重写所有查询 / Always rewrite all queries |
| `QUERY_EXPANSION` | 启用查询扩展 / Enable query expansion |
| `QUERY_EXPANSION_METHOD` | 扩展方法 (semantic/synonyms) / Expansion method |
| `BM25_K1` / `BM25_B` | BM25 参数 / BM25 parameters |
| `RERANKER_MODEL` | 重排序模型 / Reranker model |
| `RERANKER_PRELOAD` | 预加载模型 / Preload model |

### New APIs Section (Add to existing)

```
### Evaluation APIs (新增)

- `GET /evaluation/recent`: 获取最近评估记录 / Get recent evaluation records
- `GET /evaluation/summary`: 评估指标汇总 / Evaluation metrics summary
- `POST /evaluation/run`: 手动触发评估 / Trigger batch evaluation
- `GET /metrics/ragas`: RAGAS 指标趋势 / RAGAS metrics trends
```

## Implementation Steps

1. Rewrite entire README.md with bilingual format
2. Add placeholder sections for diagrams
3. Document all configuration variables
4. Document all API endpoints (including new evaluation APIs)
5. Add feature descriptions with bilingual format
6. Keep existing valid content (Offline Ingest, Query Example, Notes)

## Files to Modify

| File | Action |
|------|--------|
| `README.md` | Rewrite with bilingual format, add new content |

## Validation

- All environment variables documented
- All API endpoints documented
- Bilingual format consistent (CN/EN)
- Diagram placeholders clearly marked